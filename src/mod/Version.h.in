#pragma once

// clang-format off

#define VERSION_CONFIG      ${VERSION_MAJOR}${VERSION_MINOR}${VERSION_ALTER}

#define VERSION_MAJOR       ${VERSION_MAJOR}
#define VERSION_MINOR       ${VERSION_MINOR}
#define VERSION_ALTER       ${VERSION_ALTER}

#define BUILD_MODE          "${MODE}"
#define BUILD_COMMIT_SHA    "${GIT_COMMIT}"

#define VERSION_TO_STRING_INNER(ver) #ver
#define VERSION_TO_STRING(ver)  VERSION_TO_STRING_INNER(ver)

#define VERSION_STRING                   \
    VERSION_TO_STRING(VERSION_MAJOR)     \
    "." VERSION_TO_STRING(VERSION_MINOR) \
    "." VERSION_TO_STRING(VERSION_ALTER) \

#define BUILD_INFO_STRING                    \
    "[" VERSION_TO_STRING(BUILD_CHANNEL) "]" \
    " " VERSION_STRING                       \
    "+" BUILD_COMMIT_SHA                     \
    "-" BUILD_MODE   

#define QML_PACKAGE_NAME          "com.github.penuniverse"
#define QML_PACKAGE_VERSION_MAJOR 1
#define QML_PACKAGE_VERSION_MINOR 0

// clang-format on
